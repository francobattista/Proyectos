function circleArea(radio)
{
    return Math.PI * Math.pow(radio, 2);
}


//Me da paja seguir con las otras funciones, pero es asi.,

// Franquito dejate de olvidar los exports como en OSSE
module.exports = {
    circleArea: circleArea
}